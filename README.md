# YouTubeTranscriptExtractor
This is a Python-based tool that extracts and saves transcripts from individual YouTube videos or entire playlists without using the YouTube Data API. It fetches available subtitles (either auto-generated or uploaded) and saves them as .txt files named after the video titles. ⚠️ It only works for YouTube videos that have transcripts available.
